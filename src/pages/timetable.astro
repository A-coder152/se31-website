---
import Layout from "../layouts/Layout.astro";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import PageIntro from "../components/PageIntro.astro";
import { TIMETABLE, TIMETABLE_META } from "../data/timetable";
import { formatTorontoDate } from "../utils/date";

const days = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"];

const START = 8 * 60 + 30;
const SLOT_HEIGHT = 40;
function toMinutes(time: string) {
	const [h, m] = time.split(":").map(Number);
	return h * 60 + m;
}

function top(time: string) {
	return ((toMinutes(time) - START) / 30) * SLOT_HEIGHT;
}

function height(start: string, end: string) {
	return ((toMinutes(end) - toMinutes(start)) / 30) * SLOT_HEIGHT;
}

const times = Array.from({ length: 19 }, (_, i) => {
	const mins = START + i * 30;
	const h = Math.floor(mins / 60);
	const m = mins % 60;
	return `${String(h).padStart(2, "0")}:${String(m).padStart(2, "0")}`;
});

const missingLocationCount = TIMETABLE.filter((event) => !event.location).length;
---

<Layout title={`${TIMETABLE_META.term} Timetable - SE31 Build Log`} description="An unofficial Fall 2026 planning draft for incoming SE31 students, with Quest linked as the current official source.">
	<Navbar />

	<main id="main-content" tabindex="-1">
		<PageIntro
			eyebrow={`Timetable / ${TIMETABLE_META.status}`}
			title={`${TIMETABLE_META.term} weekly timetable.`}
			description={TIMETABLE_META.audience}
		>
			<div class="mt-8 max-w-4xl rounded-xl border border-signal/70 bg-[#fff7d5] p-5 text-sm leading-6 text-ink dark:border-signal/50 dark:bg-[#332b12] dark:text-paper">
				<p class="font-black">Unofficial schedule — do not rely on it yet.</p>
				<p>{TIMETABLE_META.notice}</p>
				<p class="mt-3 font-mono text-xs font-bold uppercase tracking-[0.08em]">
					{missingLocationCount} of {TIMETABLE.length} locations unconfirmed · Last checked {formatTorontoDate(TIMETABLE_META.lastChecked)} ·
					{" "}<a href={TIMETABLE_META.sourceUrl} target="_blank" rel="noopener noreferrer" class="brand-text-link focus-visible:outline-spruce dark:focus-visible:outline-lime">{TIMETABLE_META.sourceLabel}</a>
				</p>
			</div>
		</PageIntro>

		<div class="mx-auto max-w-7xl px-4 py-12 sm:px-6 sm:py-16 lg:px-8">
		<section aria-labelledby="agenda-heading" class="lg:sr-only">
			<h2 id="agenda-heading" class="mb-4 text-2xl font-bold">Daily agenda</h2>
			<div class="space-y-6">
				{days.map((day) => (
					<section aria-labelledby={`agenda-${day.toLowerCase()}`}>
						<h3 id={`agenda-${day.toLowerCase()}`} class="mb-2 text-xl font-bold">{day}</h3>
						<ol class="space-y-2">
							{TIMETABLE.filter((event) => event.day === day).map((event) => (
								<li class={`rounded-xl border border-black/10 p-3 shadow-sm dark:border-white/15 ${event.color}`}>
									<strong class="text-lg">{event.course} {event.type}</strong>
									<p class="mt-1 text-sm">
										<time datetime={event.start}>{event.start}</time>–<time datetime={event.end}>{event.end}</time>
										<span aria-hidden="true"> · </span>
										<span>{event.location || "Location not confirmed"}</span>
									</p>
								</li>
							))}
						</ol>
					</section>
				))}
			</div>
		</section>

		<div class="hidden overflow-x-auto rounded-xl border border-[#cbd4cc] shadow-[0_12px_32px_rgb(16_33_27_/_0.08)] lg:block dark:border-night-border" aria-hidden="true">
			<div
				class="
					grid
					min-w-[930px]
					grid-cols-[80px_repeat(5,minmax(170px,1fr))]
					grid-rows-[44px_auto]
					border-0
					bg-white
					dark:border-gray-600
					dark:bg-gray-900
				"
			>
				<div class="border-b border-r border-gray-300 dark:border-gray-600"></div>

				{
					days.map(day => (
						<div
							class="
								flex
								h-11
								items-center
								justify-center
								border-b
								border-l
								border-gray-300
								bg-gray-100
								font-bold
								dark:border-gray-600
								dark:bg-gray-800
								dark:text-gray-100
							"
						>
							{day}
						</div>
					))
				}

				<div class="flex flex-col bg-white dark:bg-gray-900">
					{
						times.map(time => (
							<div
								class="
									flex
									h-10
									items-center
									justify-center
									border-b
									border-r
									border-gray-200
									text-sm
									dark:border-gray-700
									dark:text-gray-300
								"
							>
								{time}
							</div>
						))
					}
				</div>

				{
					days.map(day => (
						<div
							class="
								relative
								flex
								flex-col
								border-l
								border-gray-300
								bg-white
								dark:border-gray-600
								dark:bg-gray-900
							"
						>
							{
								times.map(() => (
									<div
										class="
											h-10
											border-b
											border-gray-200
											box-border
											dark:border-gray-700
										"
									></div>
								))
							}

							{
								TIMETABLE
									.filter(e => e.day === day)
									.map(event => (
										<div
											class:list={[
												`
													absolute
													left-[1px]
													right-[1px]
													z-[5]
													box-border
													flex
													flex-col
													justify-center
													overflow-hidden
													rounded-lg
													border
													border-black/10
													px-2
													py-1
													text-[0.8rem]
													leading-tight
													shadow-sm
													dark:border-white/15
												`,
											event.color,
											]}
											style={`
												top:${top(event.start)}px;
												height:${height(event.start, event.end)}px;
											`}
										>
											<div class="mb-1 font-bold">
												{event.course} {event.type}
											</div>
							
											<div class="mt-0.5 text-[0.72rem] opacity-80">
												{event.location || "Location TBD"}
											</div>
										</div>
									))
							}
						</div>
					))
				}
			</div>
		</div>
		</div>
	</main>

	<Footer />
</Layout>
