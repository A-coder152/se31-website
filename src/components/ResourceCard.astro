---
import type { Resource } from "../data/types";
import Badge from "./Badge.astro";

interface Props {
  resource: Resource;
}

const { resource } = Astro.props;

const categoryStyleMap: Record<string, { color: string; textColor: string }> = {
  University: { color: "bg-indigo-100 dark:bg-indigo-950", textColor: "text-indigo-800 dark:text-indigo-200" },
  Academics: { color: "bg-red-100 dark:bg-red-950", textColor: "text-red-800 dark:text-red-200" },
  "Co-op": { color: "bg-blue-100 dark:bg-blue-950", textColor: "text-blue-800 dark:text-blue-200" },
  "Student life": { color: "bg-green-100 dark:bg-green-950", textColor: "text-green-800 dark:text-green-200" },
  "Developer tools": { color: "bg-purple-100 dark:bg-purple-950", textColor: "text-purple-800 dark:text-purple-200" },
  Community: { color: "bg-yellow-100 dark:bg-yellow-950", textColor: "text-yellow-900 dark:text-yellow-200" },
};

const categoryStyle = categoryStyleMap[resource.category] ?? {
  color: "bg-gray-200 dark:bg-gray-700",
  textColor: "text-gray-800 dark:text-gray-100",
};
---

<a href={resource.url} target="_blank" rel="noopener noreferrer" class="brand-card group flex flex-col border-t-4 border-t-spruce p-6 transition-transform duration-200 hover:-translate-y-1 hover:border-spruce focus-visible:outline focus-visible:outline-3 focus-visible:outline-offset-4 focus-visible:outline-spruce motion-reduce:transform-none motion-reduce:transition-none dark:border-t-lime dark:hover:border-lime dark:focus-visible:outline-lime">
  <div class="flex justify-between items-start mb-2">
    <h3 class="mr-5 min-w-0 text-xl font-black tracking-[-0.02em] text-ink dark:text-paper">
      {resource.title}
    </h3>
    <Badge text={resource.category} color={categoryStyle.color} textColor={categoryStyle.textColor} />
  </div>
  <p class="mb-4 flex-grow leading-7 text-muted dark:text-[#b8c5be]">
    {resource.description}
  </p>
  {resource.official && (
    <div class="mt-2">
      <Badge text="Official Waterloo source" color="bg-[#d8eee5] dark:bg-[#163a2e]" textColor="text-spruce dark:text-[#a9e8cc]" />
    </div>
  )}
</a>
